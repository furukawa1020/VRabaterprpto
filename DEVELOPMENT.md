# VRabater 開発ガイド 🛠️

このドキュメントは、VRabaterのコードベースを理解し、機能を拡張したい開発者向けです。

## 📐 アーキテクチャ

```
┌─────────────────────────────────────────────────────────┐
│                    ユーザー                              │
│  (Webブラウザ + Webカメラ + マイク)                      │
└────────────────┬────────────────────────────────────────┘
                 │
    ┌────────────┼────────────────┐
    │            │                │
    ▼            ▼                ▼
┌────────┐  ┌─────────┐    ┌──────────┐
│ three  │  │ WebAudio│    │ WebSocket│
│  +VRM  │  │ (Tone.js│    │  Client  │
└───┬────┘  └────┬────┘    └─────┬────┘
    │            │               │
    │            │               │
    ▼            ▼               ▼
┌──────────────────────────────────────┐
│     Web UI (apps/web)                │
│   - AvatarSystem (描画・制御)        │
│   - AudioProcessor (音声処理)        │
│   - TrackingClient (データ受信)      │
│   - UI (コントロールパネル)          │
└─────────┬────────────────────────────┘
          │
          ▼ WebSocket
┌──────────────────────────┐
│  Gateway (apps/gateway)  │
│  - OSCサーバー           │
│  - WebSocketブリッジ     │
└─────────┬────────────────┘
          │
          ▼ OSC
┌──────────────────────────┐
│  OpenSeeFace             │
│  (外部プロセス)          │
└──────────────────────────┘

          │
          ▼ HTTP REST API
┌──────────────────────────┐
│  AI Service (apps/ai)    │
│  - STT (Whisper/Vosk)    │
│  - LLM (Ollama)          │
│  - TTS (Piper)           │
└──────────────────────────┘
```

---

## 🗂️ ディレクトリ構造の詳細

```
VRabater/
├── apps/
│   ├── web/                    # フロントエンド（three.js + TypeScript）
│   │   ├── src/
│   │   │   ├── avatar/
│   │   │   │   └── AvatarSystem.ts    # VRM制御・描画・アニメーション
│   │   │   ├── audio/
│   │   │   │   └── AudioProcessor.ts  # 音声入力・変換・リップシンク
│   │   │   ├── tracking/
│   │   │   │   ├── TrackingClient.ts  # WebSocket受信
│   │   │   │   └── types.ts           # トラッキングデータ型定義
│   │   │   ├── ui/
│   │   │   │   └── UI.ts              # コントロールパネル
│   │   │   ├── config.ts              # 設定・テーマ・パラメータ
│   │   │   └── main.ts                # エントリーポイント
│   │   ├── public/
│   │   │   └── models/                # VRMファイル
│   │   ├── index.html
│   │   ├── vite.config.ts
│   │   └── package.json
│   ├── gateway/                # WebSocketゲートウェイ（Node.js）
│   │   ├── index.js            # OSC→WebSocketブリッジ
│   │   └── package.json
│   └── ai/                     # AIサービス（Python + Flask）
│       ├── main.py             # REST APIサーバー
│       ├── requirements.txt
│       ├── download_models.py
│       └── models/             # Whisper/Piperモデル保存先
├── assets/
│   ├── hdris/                  # PBR環境マップ（.hdr）
│   ├── vrm/                    # VRMモデル
│   └── fonts/                  # UIフォント
├── scripts/
│   ├── start_all.ps1           # 一括起動（Windows）
│   └── start_all.sh            # 一括起動（macOS/Linux）
├── licenses/
│   └── third_party.md          # ライセンス台帳
├── README.md                   # プロジェクト概要
├── SETUP.md                    # セットアップガイド
├── DEVELOPMENT.md              # このファイル
└── package.json                # ルートpackage.json（workspaces）
```

---

## 🧩 主要モジュールの解説

### 1. AvatarSystem (`apps/web/src/avatar/AvatarSystem.ts`)

**責務**: VRMモデルの読み込み、three.jsシーンの管理、アニメーション制御

**主要メソッド**:
- `init()`: three.js初期化、PBRレンダラー設定、照明配置
- `loadVRM(path)`: VRMモデルの読み込み
- `updateFromTracking(data)`: トラッキングデータから表情・姿勢を更新
- `updateIdleAnimation(deltaTime)`: 呼吸アニメーション
- `updateBlinking(deltaTime)`: 自動まばたき
- `startAnimation()`: レンダリングループ開始

**カスタマイズポイント**:
- **照明変更**: `setupLighting()` でライトの色・位置・強度を調整
- **アイドル動作**: `CONFIG.avatar.idle` で呼吸周期・揺れ幅を変更
- **表情カーブ**: `CONFIG.avatar.expression.blinkCurve` でまばたきの動きを変更

### 2. AudioProcessor (`apps/web/src/audio/AudioProcessor.ts`)

**責務**: マイク入力、ボイスチェンジャー、リップシンク、デバイス管理

**主要メソッド**:
- `init()`: AudioContext・Tone.js初期化、マイクアクセス
- `enableVoiceChanger(enable)`: ピッチシフト有効化
- `setPitchShift(semitones)`: ピッチ変更（半音単位）
- `onLipSync(callback)`: リップシンク用コールバック登録
- `startLipSyncMonitoring()`: 音量モニタリング開始

**今後の拡張**:
- フォルマントシフト（WASM: Rubber Band / World）
- RVC音声変換（GPU必要）

### 3. TrackingClient (`apps/web/src/tracking/TrackingClient.ts`)

**責務**: Gateway（WebSocket）との通信、トラッキングデータの受信

**主要メソッド**:
- `connect()`: WebSocket接続
- `on(event, callback)`: イベントリスナー登録
- `attemptReconnect()`: 自動再接続

**データフロー**:
```
OpenSeeFace → OSC → Gateway → WebSocket → TrackingClient → AvatarSystem
```

### 4. UI (`apps/web/src/ui/UI.ts`)

**責務**: コントロールパネルの描画、ユーザー操作の処理

**カスタマイズポイント**:
- `createControls()`: HTMLの構造を変更
- `injectStyles()`: CSSスタイルをカスタマイズ
- `THEME.colors`: 白山カラーパレットを変更

---

## 🎨 デザインシステム

### カラーパレット（`config.ts` の `THEME.colors`）

| 色名 | HEX | 用途 | モチーフ |
|------|-----|------|----------|
| snowWhite | `#F7F7F7` | 背景・ハイライト | 白山の雪 |
| riverCyan | `#1E6F68` | プライマリ・アクセント | 手取川の碧 |
| rockBlack | `#2E2B2B` | 背景・シャドウ | 玄武岩 |
| woodBrown | `#A67C52` | セカンダリ・暖色 | 里山材 |

### モチーフの実装例

- **川の蛇行（meander）**: 髪の曲線、UIの波線
- **雪の粒子**: パーティクルシステム（将来実装）
- **六角形**: ボタン・アイコンのシェイプ
- **透明感**: PBRマテリアル、ガラス質感

---

## 🔧 設定ファイル（`apps/web/src/config.ts`）

### 主要パラメータ

```typescript
export const CONFIG = {
  avatar: {
    rendering: {
      toneMapping: 'ACESFilmic',        // トーンマッピング
      toneMappingExposure: 1.0,         // 露出
    },
    expression: {
      smoothingFactor: 0.35,            // 表情のスムージング（EMA係数）
      blinkInterval: { min: 2.5, max: 5.0 }, // まばたき間隔（秒）
    },
    idle: {
      breathingCycle: 5.0,              // 呼吸周期（秒）
      breathingAmplitude: 0.015,        // 呼吸の振幅
    },
  },
  audio: {
    voiceChanger: {
      pitchShift: 3.5,                  // ピッチシフト（半音）
      formantShift: 1.2,                // フォルマント倍率（未実装）
    },
  },
  ai: {
    llm: {
      model: 'qwen2.5:3b-instruct-q4_K_M', // Ollamaモデル
      temperature: 0.8,                 // 創造性
      maxTokens: 100,                   // 最大トークン数
    },
  },
};
```

---

## 🚀 新機能の追加方法

### 例: 新しい表情プリセットを追加

1. **VRMモデルにブレンドシェイプを追加**（Blender/VRoid Studio）
2. `AvatarSystem.ts` に適用ロジックを追加:

```typescript
setExpression(name: string, value: number) {
  if (!this.vrm?.expressionManager) return;
  this.vrm.expressionManager.setValue(name, value);
}
```

3. `UI.ts` にボタンを追加:

```typescript
<button class="ui-button" data-expression="surprised">😲</button>
```

4. イベントリスナーで処理:

```typescript
const presets: Record<string, Record<string, number>> = {
  surprised: { surprised: 1.0, eyebrowUp: 0.8 },
};
```

### 例: 新しいHDRIプリセットを追加

1. HDRIファイルを `assets/hdris/` に配置
2. `config.ts` に追加:

```typescript
hdri: {
  presets: {
    forest: '/hdris/forest_path.hdr',
  },
},
```

3. `UI.ts` のセレクトボックスに追加:

```html
<option value="forest">森の小道</option>
```

---

## 🧪 テストとデバッグ

### パフォーマンス計測

```typescript
// apps/web/src/main.ts
const stats = new Stats();
document.body.appendChild(stats.dom);

// アニメーションループ内
stats.begin();
renderer.render(scene, camera);
stats.end();
```

### ブラウザ開発ツール

- **F12** → Console: エラーログ確認
- **Performance** タブ: FPS計測
- **Network** タブ: WebSocket通信確認

### デバッグモード

`config.ts` に追加:

```typescript
export const DEBUG = {
  showStats: true,
  logTracking: false,
  logAudio: false,
};
```

---

## 📦 ビルドとデプロイ

### 開発ビルド

```powershell
cd apps/web
npm run dev
```

### 本番ビルド

```powershell
cd apps/web
npm run build
# 出力: dist/ フォルダ
```

### 静的ホスティング（将来）

`dist/` フォルダを以下にデプロイ可能:
- GitHub Pages
- Vercel
- Netlify

**注意**: AI機能はローカルのみのため、デプロイ時は無効化が必要

---

## 🤝 コントリビューション

### プルリクエストの流れ

1. Issue作成（機能提案・バグ報告）
2. Fork & ブランチ作成
3. コード変更 + テスト
4. `licenses/third_party.md` 更新（新規アセット追加時）
5. プルリクエスト作成

### コーディング規約

- **TypeScript**: `eslint` + `prettier`
- **命名**: camelCase（変数・関数）、PascalCase（クラス）
- **コメント**: 日本語OK、複雑なロジックは説明を追加

---

## 📚 参考資料

### 公式ドキュメント

- [three.js](https://threejs.org/docs/)
- [three-vrm](https://github.com/pixiv/three-vrm)
- [Tone.js](https://tonejs.github.io/)
- [Ollama API](https://github.com/ollama/ollama/blob/main/docs/api.md)
- [VRM Specification](https://vrm.dev/en/)

### 学習リソース

- PBRレンダリング: https://learnopengl.com/PBR/Theory
- Web Audio API: https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API
- OSC Protocol: http://opensoundcontrol.org/

---

## 🐛 既知の問題

- [ ] フォルマントシフト未実装（Tone.jsの制約）
- [ ] TTS (Piper) 未統合
- [ ] macOS Safari での WebAudio 制約
- [ ] HDRI フォールバックが簡素

---

## 🎯 ロードマップ

### v1.1
- [ ] Piper TTS 統合
- [ ] RVC音声変換（オプション・GPU）
- [ ] 表情プリセット追加

### v1.2
- [ ] パーティクルシステム（雪・霧）
- [ ] 背景カスタマイズ
- [ ] VRM着せ替え機能

### v2.0
- [ ] WebRTC 多人数ルーム
- [ ] 感情推定（HRV）
- [ ] モバイル対応

---

開発で困ったことがあれば、GitHub Issuesで質問してください！ 🚀
